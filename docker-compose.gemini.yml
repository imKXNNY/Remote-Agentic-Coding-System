services:
  # Use this for external databases (Supabase, Neon, etc.)
  # Command: docker-compose --profile external-db up
  app:
    profiles: ["external-db"]
    build: .
    env_file: .env
    ports:
      # Use a different host port so this stack can run beside the default compose setup.
      - "${GEMINI_APP_PORT:-3100}:3000"
    volumes:
      - ${WORKSPACE_PATH:-./workspace}:/workspace
      - ${GEMINI_CREDS_PATH:-./.gemini}:/home/appuser/.gemini
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1

  # Use this for local Docker database
  # Command: docker-compose --profile with-db up
  app-with-db:
    profiles: ["with-db"]
    build: .
    env_file: .env
    environment:
      # Override DATABASE_URL to use Docker service name
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/remote_coding_agent
    ports:
      # Use a different host port so this stack can run beside the default compose setup.
      - "${GEMINI_APP_PORT:-3100}:3000"
    volumes:
      - ${WORKSPACE_PATH:-./workspace}:/workspace
      - ${GEMINI_CREDS_PATH:-./.gemini}:/home/appuser/.gemini
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=1
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:18
    profiles: ["with-db"]
    environment:
      POSTGRES_DB: remote_coding_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql
      - ./migrations:/docker-entrypoint-initdb.d
    ports:
      # Offset host postgres port as well to avoid colliding with other stacks.
      - "${GEMINI_POSTGRES_PORT:-5543}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
